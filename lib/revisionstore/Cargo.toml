[package]
name = "revisionstore"
version = "0.1.0"
authors = ["Facebook Source Control Team <sourcecontrol-dev@fb.com>"]

[profile.release]
lto = true

[dependencies]
failure = "*"
mpatch = { path = "../mpatch" }
