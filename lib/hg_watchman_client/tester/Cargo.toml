[package]
name = "tester"
version = "0.1.0"
edition = "2018"

[dependencies]
hg_watchman_client = { version = "0.1.0", path = "../" }
watchman_client = { version = "0.1.0", path = "../../watchman_client/" }
clap = "2.32.0"
serde = "1"
serde_derive = "1"
failure = "0.1.3"

[features]
default = []
debug_bytes = ["hg_watchman_client/debug_bytes"]
