[package]
name = "procinfo"
version = "0.0.1"
edition = "2018"

[dependencies]
libc = "0.2"

[target.'cfg(windows)'.dependencies]
kernel32-sys = "0.2.2"
winapi = "0.2.8"

[target.'cfg(target_os = "macos")'.build-dependencies]
cc = "1.0"
