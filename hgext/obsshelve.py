# This is a temporary workaround until T19372873 gets closed.
# In a nutshell, because we have some messed up configs, we need a dummy
# extension file to exist, so that `extensions.obsshelve=` in configs does
# not cause any warnings
