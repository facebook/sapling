# This file needs to be named "getdb.sh" to be effective.
# Below is an example. Change the values for your test environment.

DBHOST=localhost
DBPORT=3306
DBNAME=infinitepush_testdb
DBUSER=root
DBPASS=

echo "$DBHOST:$DBPORT:$DBNAME:$DBUSER:$DBPASS"
