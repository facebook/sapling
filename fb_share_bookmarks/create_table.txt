drop table if exists hgBookmarks;

create table hgBookmarks (
  name varchar(128) not null,
  server varchar(64) not null,
  path varchar(256) not null,
  primary key (name),
  constraint unique index idx_bookmarks_unique (name)
);

