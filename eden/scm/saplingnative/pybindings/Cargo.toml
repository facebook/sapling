# @generated by autocargo from //eden/scm/saplingnative/pybindings:bindings-lib

[package]
name = "bindings-lib"
version = "0.1.0"
edition = "2021"

[lib]
test = false
doctest = false

[dependencies]
commands = { version = "0.1.0", path = "../../lib/commands" }
cpython = { version = "0.7.2", features = ["extension-module", "python3-sys", "serde-convert"], default-features = false }
