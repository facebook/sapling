# @generated by autocargo

[package]
name = "manifest"
version = "0.1.0"
authors = ["Facebook Source Control Team <sourcecontrol-dev@fb.com>"]
edition = "2021"

[dependencies]
anyhow = "1.0.65"
pathmatcher = { version = "0.1.0", path = "../pathmatcher" }
quickcheck = { version = "1.0", optional = true }
quickcheck_arbitrary_derive = { version = "0.1.0", git = "https://github.com/facebookexperimental/rust-shed.git", branch = "main", optional = true }
types = { version = "0.1.0", path = "../types" }

[dev-dependencies]
quickcheck = "1.0"
quickcheck_arbitrary_derive = { version = "0.1.0", git = "https://github.com/facebookexperimental/rust-shed.git", branch = "main" }
types = { version = "0.1.0", path = "../types", features = ["for-tests"], default-features = false }

[features]
default = []
for-tests = ["quickcheck", "quickcheck_arbitrary_derive"]
