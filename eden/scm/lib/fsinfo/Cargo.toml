# @generated by autocargo

[package]
name = "fsinfo"
version = "0.1.0"
edition = "2021"

[dependencies]
anyhow = "1.0.56"
libc = "=0.2.125"

[target.'cfg(target_os = "windows")'.dependencies]
winapi = { version = "0.3", features = ["everything"] }

[target.'cfg(windows)'.dependencies]
winapi = { version = "0.3", features = ["fileapi", "handleapi", "ioapiset", "minwindef", "winbase", "winerror", "winioctl"] }
