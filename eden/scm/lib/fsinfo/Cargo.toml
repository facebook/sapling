# @generated by autocargo from //eden/scm/lib/fsinfo:fsinfo

[package]
name = "sapling-fsinfo"
version = "0.1.0"
authors = ["Meta Source Control Team <sourcecontrol-dev@meta.com>"]
edition = "2024"
homepage = "https://sapling-scm.com/"
repository = "https://github.com/facebook/sapling"
license = "MIT"

[lib]
name = "fsinfo"

[dependencies]
anyhow = "1.0.98"

[target.'cfg(target_os = "linux")'.dependencies]
libc = "0.2.139"

[target.'cfg(target_os = "macos")'.dependencies]
libc = "0.2.139"

[target.'cfg(target_os = "windows")'.dependencies]
sapling-identity = { version = "0.1.0", path = "../identity" }
winapi = { version = "0.3", features = ["everything", "std"] }

[target.'cfg(windows)'.dependencies]
winapi = { version = "0.3", features = ["fileapi", "handleapi", "ioapiset", "minwindef", "winbase", "winerror", "winioctl"] }
