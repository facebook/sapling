# @generated by autocargo from //eden/scm/lib/renderdag:renderdag

[package]
name = "renderdag"
version = "0.1.0"
edition = "2021"
license = "MIT"

[dependencies]
bitflags = { version = "2.6", features = ["serde"] }
serde = { version = "1.0.185", features = ["derive", "rc"], optional = true }

[dev-dependencies]
dag = { version = "0.1.0", path = "../dag", default-features = false }
nonblocking = { version = "0.1.0", path = "../nonblocking" }
unicode-width = "=0.1.12"

[features]
default = []
serialize = ["serde"]
