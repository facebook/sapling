# @generated by autocargo from //eden/scm/lib/commandserver:commandserver

[package]
name = "sapling-commandserver"
version = "0.1.0"
authors = ["Meta Source Control Team <sourcecontrol-dev@meta.com>"]
edition = "2021"
homepage = "https://sapling-scm.com/"
repository = "https://github.com/facebook/sapling"
license = "MIT"

[lib]
name = "commandserver"

[dependencies]
anyhow = "1.0.86"
dirs = "2.0"
fn-error-context = "0.2"
fs2 = "0.4"
once_cell = "1.12"
sapling-configmodel = { version = "0.1.0", path = "../config/model" }
sapling-identity = { version = "0.1.0", path = "../identity" }
sapling-nodeipc = { version = "0.1.0", path = "../util/nodeipc" }
sapling-spawn-ext = { version = "0.1.0", path = "../spawn-ext" }
sapling-udsipc = { version = "0.1.0", path = "../util/udsipc" }
sapling-version = { version = "0.1.0", path = "../version" }
serde = { version = "1.0.185", features = ["derive", "rc"] }
tracing = { version = "0.1.40", features = ["attributes", "valuable"] }

[target.'cfg(target_os = "linux")'.dependencies]
libc = "0.2.139"

[target.'cfg(target_os = "macos")'.dependencies]
libc = "0.2.139"

[target.'cfg(target_os = "windows")'.dependencies]
sapling-atexit = { version = "0.1.0", path = "../util/atexit" }
sapling-procutil = { version = "0.1.0", path = "../util/procutil" }
