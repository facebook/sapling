# @generated by autocargo from //eden/scm/lib/commandserver:commandserver

[package]
name = "commandserver"
version = "0.1.0"
edition = "2021"

[dependencies]
anyhow = "1.0.75"
configmodel = { version = "0.1.0", path = "../config/model" }
dirs = "2.0"
fn-error-context = "0.2"
fs2 = "0.4"
identity = { version = "0.1.0", path = "../identity" }
nodeipc = { version = "0.1.0", path = "../util/nodeipc" }
once_cell = "1.12"
serde = { version = "1.0.185", features = ["derive", "rc"] }
spawn-ext = { version = "0.1.0", path = "../spawn-ext" }
system-command = { version = "0.1.0", path = "../util/system-command" }
tracing = "0.1.40"
udsipc = { version = "0.1.0", path = "../util/udsipc" }
version = { version = "0.1.0", path = "../version" }

[target.'cfg(target_os = "linux")'.dependencies]
libc = "0.2.139"

[target.'cfg(target_os = "macos")'.dependencies]
libc = "0.2.139"

[target.'cfg(target_os = "windows")'.dependencies]
atexit = { version = "0.1.0", path = "../util/atexit" }
procutil = { version = "0.1.0", path = "../util/procutil" }
