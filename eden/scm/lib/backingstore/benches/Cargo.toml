# @generated by autocargo from //eden/scm/lib/backingstore/benches:backingstore-benches

[package]
name = "backingstore-benches"
version = "0.1.0"
edition = "2021"

[dependencies]
backingstore = { version = "0.1.0", path = ".." }
configloader = { version = "0.1.0", path = "../../config/loader" }
identity = { version = "0.1.0", path = "../../identity" }
minibench = { version = "0.1.0", path = "../../minibench" }
procinfo = { version = "0.1.0", path = "../../procinfo" }
repo = { version = "0.1.0", path = "../../repo" }
tempfile = "3.8"
types = { version = "0.1.0", path = "../../types" }

[features]
fb = ["configloader/fb"]
