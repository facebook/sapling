# @generated by autocargo from //eden/scm/lib/edenfs_ffi:edenfs_ffi

[package]
name = "edenfs_ffi"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["lib", "staticlib"]

[dependencies]
anyhow = "1.0.75"
async-runtime = { version = "0.1.0", path = "../async-runtime" }
cxx = "1.0.119"
fbinit = { version = "0.1.2", git = "https://github.com/facebookexperimental/rust-shed.git", branch = "main", optional = true }
identity = { version = "0.1.0", path = "../identity" }
manifest = { version = "0.1.0", path = "../manifest" }
once_cell = "1.12"
parking_lot = { version = "0.12.1", features = ["send_guard"] }
pathmatcher = { version = "0.1.0", path = "../pathmatcher" }
repo = { version = "0.1.0", path = "../repo" }
sparse = { version = "0.1.0", path = "../sparse" }
stats = { version = "0.1.0", git = "https://github.com/facebookexperimental/rust-shed.git", branch = "main", optional = true }
tracing = { version = "0.1.40", features = ["attributes"] }
types = { version = "0.1.0", path = "../types" }

[build-dependencies]
cxx-build = "1.0.119"

[features]
ods = ["fbinit", "stats"]
