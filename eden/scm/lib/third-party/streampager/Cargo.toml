# @generated by autocargo from //eden/scm/lib/third-party/streampager:streampager

[package]
name = "sapling-streampager"
version = "0.12.0"
authors = ["Mark Juggurnauth-Thomas <markbt@efaref.net>", "Facebook Source Control Team <sourcecontrol-dev@fb.com>"]
edition = "2024"
description = "streampager is a pager for command output or large files"
license = "MIT"
keywords = ["less", "more", "pager"]
categories = ["command-line-utilities", "text-processing"]

[lib]
name = "streampager"
edition = "2021"

[dependencies]
dirs = "6.0"
enum_dispatch = "0.3.13"
lru = { version = "0.16", default-features = false }
memmap2 = "0.9.5"
notify = { version = "6.1.1", optional = true }
regex = "1.12.2"
scopeguard = "1.2.0"
serde = { version = "1.0.219", features = ["derive"] }
smallvec = { version = "1.15", default-features = false }
terminfo = "0.9"
termwiz = { version = "0.23", default-features = false }
thiserror = "2.0.12"
toml = { version = "0.9.8", features = ["preserve_order"], optional = true }
unicode-segmentation = "1.12.0"
unicode-width = "0.1.12"
vec_map = "0.8"

[features]
keymap-file = []
load_file = ["notify"]
toml_config = ["toml"]
