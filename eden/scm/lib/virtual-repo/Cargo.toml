# @generated by autocargo from //eden/scm/lib/virtual-repo:virtual-repo

[package]
name = "sapling-virtual-repo"
version = "0.1.0"
authors = ["Meta Source Control Team <sourcecontrol-dev@meta.com>"]
edition = "2024"
description = "Generate source control repo data on demand for testing purpose."
homepage = "https://sapling-scm.com/"
repository = "https://github.com/facebook/sapling"
license = "MIT"

[lib]
name = "virtual_repo"

[dependencies]
anyhow = "1.0.98"
async-trait = "0.1.86"
sapling-dag = { version = "0.1.0", path = "../dag" }
sapling-eagerepo-trait = { version = "0.1.0", path = "../eagerepo/trait" }
sapling-factory = { version = "0.1.0", path = "../util/factory" }
sapling-format-util = { version = "0.1.0", path = "../util/format-util" }
sapling-minibytes = { version = "0.1.0", path = "../minibytes" }
sapling-storemodel = { version = "0.1.0", path = "../storemodel" }
sapling-types = { version = "0.1.0", path = "../types" }
sapling-virtual-tree = { version = "0.1.0", path = "virtual-tree" }
