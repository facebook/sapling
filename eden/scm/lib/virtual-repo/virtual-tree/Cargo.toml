# @generated by autocargo from //eden/scm/lib/virtual-repo/virtual-tree:virtual-tree

[package]
name = "sapling-virtual-tree"
version = "0.1.0"
authors = ["Meta Source Control Team <sourcecontrol-dev@meta.com>"]
edition = "2024"
description = "Generate source control trees on demand for testing purpose."
homepage = "https://sapling-scm.com/"
repository = "https://github.com/facebook/sapling"
license = "MIT"

[lib]
name = "virtual_tree"

[dependencies]
parking_lot = { version = "0.12.1", features = ["send_guard"] }
sapling-vlqencoding = { version = "0.1.0", path = "../../vlqencoding" }
sapling-zstdelta = { version = "0.1.0", path = "../../zstdelta" }
smallvec = { version = "1.15", default-features = false }

[profile.release]
overflow-checks = true
