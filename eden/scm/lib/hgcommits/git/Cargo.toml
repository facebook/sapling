# @generated by autocargo from //eden/scm/lib/hgcommits/git:commits-git

[package]
name = "commits-git"
version = "0.1.0"
edition = "2021"

[dependencies]
anyhow = "1.0.75"
async-trait = "0.1.71"
commits-trait = { version = "0.1.0", path = "../trait" }
dag = { version = "0.1.0", path = "../../dag" }
factory = { version = "0.1.0", path = "../../util/factory" }
fs-err = { version = "2.6.0", features = ["tokio"] }
futures = { version = "0.3.28", features = ["async-await", "compat"] }
gitdag = { version = "0.1.0", path = "../../dag/gitdag" }
metalog = { version = "0.1.0", path = "../../metalog" }
minibytes = { version = "0.1.0", path = "../../minibytes" }
parking_lot = { version = "0.12.1", features = ["send_guard"] }
refencode = { version = "0.1.0", path = "../../refencode" }
storemodel = { version = "0.1.0", path = "../../storemodel" }
tracing = "0.1.40"
types = { version = "0.1.0", path = "../../types" }
