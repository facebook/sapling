# @generated by autocargo from //eden/scm/lib/worktree:worktree

[package]
name = "sapling-worktree-registry"
version = "0.1.0"
authors = ["Meta Source Control Team <sourcecontrol-dev@meta.com>"]
edition = "2024"
homepage = "https://sapling-scm.com/"
repository = "https://github.com/facebook/sapling"
license = "GPL-2.0-only"

[lib]
name = "worktree"

[dependencies]
anyhow = "1.0.102"
chrono = { version = "0.4.44", features = ["clock", "serde", "std"], default-features = false }
fs-err = { version = "3.3.0", features = ["tokio"] }
sapling-util = { version = "0.1.0", path = "../util" }
serde = { version = "1.0.219", features = ["derive", "rc"] }
serde_json = { version = "1.0.140", features = ["alloc", "float_roundtrip", "raw_value", "unbounded_depth"] }

[dev-dependencies]
tempfile = "3.26.0"
