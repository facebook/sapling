# @generated by autocargo from //eden/scm/lib/dag/dag-types:dag-types

[package]
name = "dag-types"
version = "0.1.0"
edition = "2021"
license = "MIT"

[dependencies]
abomonation = { version = "0.7", features = ["smallvec"], optional = true }
abomonation_derive = "0.5"
minibytes = { version = "0.1.0", path = "../../minibytes", default-features = false }
quickcheck = { version = "1.0", optional = true }
serde = { version = "1.0.185", features = ["derive", "rc"] }

[dev-dependencies]
quickcheck = "1.0"

[features]
for-tests = ["quickcheck"]
serialize-abomonation = ["abomonation"]
