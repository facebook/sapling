# @generated by autocargo from //eden/scm/lib/dag/dag-types:dag-types

[package]
name = "sapling-dag-types"
version = "0.1.0"
authors = ["Meta Source Control Team <sourcecontrol-dev@meta.com>"]
edition = "2024"
description = "A subset of types used by sapling-dag"
homepage = "https://sapling-scm.com/"
repository = "https://github.com/facebook/sapling"
license = "MIT"

[lib]
name = "dag_types"

[dependencies]
quickcheck = { version = "1.0", optional = true }
sapling-minibytes = { version = "0.1.0", path = "../../minibytes", default-features = false }
serde = { version = "1.0.219", features = ["derive", "rc"] }

[dev-dependencies]
quickcheck = "1.0"

[features]
for-tests = ["quickcheck"]
