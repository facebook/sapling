# @generated by autocargo

[package]
name = "constructors"
version = "0.1.0"
edition = "2021"

[dependencies]
eagerepo = { version = "0.1.0", path = "../eagerepo" }
edenapi = { version = "0.1.0", path = "../edenapi" }
gitstore = { version = "0.1.0", path = "../gitstore", optional = true }
once_cell = "1.12"

[features]
default = ["git"]
git = ["gitstore"]
