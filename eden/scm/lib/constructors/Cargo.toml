# @generated by autocargo from //eden/scm/lib/constructors:constructors

[package]
name = "constructors"
version = "0.1.0"
authors = ["Meta Source Control Team <sourcecontrol-dev@meta.com>"]
edition = "2021"
homepage = "https://sapling-scm.com/"
repository = "https://github.com/facebook/sapling"
license = "MIT"

[dependencies]
commits = { version = "0.1.0", path = "../commits" }
commits-git = { version = "0.1.0", path = "../commits/git", optional = true }
eagerepo = { version = "0.1.0", path = "../eagerepo" }
edenapi = { version = "0.1.0", path = "../edenapi" }
gitstore = { version = "0.1.0", path = "../gitstore", optional = true }
manifest-tree = { version = "0.1.0", path = "../manifest-tree" }
once_cell = "1.12"

[features]
default = ["git"]
git = ["commits-git", "gitstore"]
