[package]
name = "pathmatcher"
version = "0.1.0"
edition = "2018"

[dependencies]
bitflags = "1.0"
globset = "0.4.2"
ignore = "0.4"
types = { path = "../types" }

[dev-dependencies]
tempfile = "3.0.7"
