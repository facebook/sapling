# @generated by autocargo from //eden/scm/lib/conflict:conflict
[package]
name = "conflict"
version = "0.1.0"
edition = "2018"

[dependencies]
serde = { version = "=1.0.118", features = ["derive", "rc"] }
types = { path = "../types" }
