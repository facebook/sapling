# @generated by autocargo from //eden/scm/lib/mutationstore:mutationstore

[package]
name = "sapling-mutationstore"
version = "0.1.0"
authors = ["Meta Source Control Team <sourcecontrol-dev@meta.com>"]
edition = "2024"
homepage = "https://sapling-scm.com/"
repository = "https://github.com/facebook/sapling"
license = "MIT"

[lib]
name = "mutationstore"

[dependencies]
anyhow = "1.0.98"
bitflags = { version = "2.9", features = ["serde", "std"], default-features = false }
futures = { version = "0.3.31", features = ["async-await", "compat"] }
sapling-dag = { version = "0.1.0", path = "../dag", features = ["render"] }
sapling-indexedlog = { version = "0.1.0", path = "../indexedlog" }
sapling-types = { version = "0.1.0", path = "../types" }
sapling-vlqencoding = { version = "0.1.0", path = "../vlqencoding" }

[dev-dependencies]
rand = { version = "0.8", features = ["small_rng"] }
rand_chacha = "0.3"
sapling-drawdag = { version = "0.1.0", path = "../drawdag" }
sapling-types = { version = "0.1.0", path = "../types", features = ["for-tests"], default-features = false }
tempfile = "3.22"
