# @generated by autocargo from //eden/scm/lib/commands/commands/cmdversion:cmdversion

[package]
name = "cmdversion"
version = "0.1.0"
authors = ["Meta Source Control Team <sourcecontrol-dev@meta.com>"]
edition = "2021"
homepage = "https://sapling-scm.com/"
repository = "https://github.com/facebook/sapling"
license = "GPL-2.0-only"

[dependencies]
clidispatch = { version = "0.1.0", path = "../../../clidispatch" }
cmdutil = { version = "0.1.0", path = "../../cmdutil" }
identity = { version = "0.1.0", path = "../../../identity" }
version = { version = "0.1.0", path = "../../../version" }

[features]
default = []
fb = []
