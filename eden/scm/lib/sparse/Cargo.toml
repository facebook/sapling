# @generated by autocargo from //eden/scm/lib/sparse:sparse

[package]
name = "sapling-sparse"
version = "0.1.0"
authors = ["Meta Source Control Team <sourcecontrol-dev@meta.com>"]
edition = "2021"
homepage = "https://sapling-scm.com/"
repository = "https://github.com/facebook/sapling"
license = "MIT"

[lib]
name = "sparse"

[dependencies]
anyhow = "1.0.86"
futures = { version = "0.3.30", features = ["async-await", "compat"], optional = true }
globset = { version = "0.4.13", features = ["serde1"] }
once_cell = "1.12"
regex = "1.9.2"
sapling-pathmatcher = { version = "0.1.0", path = "../pathmatcher" }
sapling-rewrite-macros = { version = "0.1.0", path = "../util/rewrite-macros" }
sapling-types = { version = "0.1.0", path = "../types" }
thiserror = "1.0.64"
tracing = { version = "0.1.40", features = ["attributes", "valuable"] }

[dev-dependencies]
tokio = { version = "1.41.0", features = ["full", "test-util", "tracing"] }

[features]
async = ["futures"]
default = []
