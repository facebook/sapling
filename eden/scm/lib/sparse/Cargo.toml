# @generated by autocargo from //eden/scm/lib/sparse:sparse

[package]
name = "sparse"
version = "0.1.0"
edition = "2021"

[dependencies]
anyhow = "1.0.86"
futures = { version = "0.3.30", features = ["async-await", "compat"], optional = true }
globset = { version = "0.4.13", features = ["serde1"] }
once_cell = "1.12"
pathmatcher = { version = "0.1.0", path = "../pathmatcher" }
regex = "1.9.2"
rewrite-macros = { version = "0.1.0", path = "../util/rewrite-macros" }
thiserror = "1.0.64"
tracing = { version = "0.1.40", features = ["attributes", "valuable"] }
types = { version = "0.1.0", path = "../types" }

[dev-dependencies]
tokio = { version = "1.41.0", features = ["full", "test-util", "tracing"] }

[features]
async = ["futures"]
default = []
