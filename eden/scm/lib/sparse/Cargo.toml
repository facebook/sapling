# @generated by autocargo from //eden/scm/lib/sparse:sparse

[package]
name = "sapling-sparse"
version = "0.1.0"
authors = ["Meta Source Control Team <sourcecontrol-dev@meta.com>"]
edition = "2024"
homepage = "https://sapling-scm.com/"
repository = "https://github.com/facebook/sapling"
license = "MIT"

[lib]
name = "sparse"

[dependencies]
anyhow = "1.0.98"
futures = { version = "0.3.31", features = ["async-await", "compat"], optional = true }
globset = { version = "0.4.13", features = ["serde1"] }
once_cell = "1.21"
regex = "1.11.1"
sapling-pathmatcher = { version = "0.1.0", path = "../pathmatcher" }
sapling-rewrite-macros = { version = "0.1.0", path = "../util/rewrite-macros" }
sapling-types = { version = "0.1.0", path = "../types" }
thiserror = "2.0.12"
tokio = { version = "1.47.1", features = ["full", "test-util", "tracing"], optional = true }
tracing = { version = "0.1.41", features = ["attributes", "valuable"] }

[dev-dependencies]
tokio = { version = "1.47.1", features = ["full", "test-util", "tracing"] }

[features]
async = ["futures", "tokio"]
default = []
