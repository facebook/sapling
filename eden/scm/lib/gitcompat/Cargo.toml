# @generated by autocargo from //eden/scm/lib/gitcompat:gitcompat

[package]
name = "gitcompat"
version = "0.1.0"
edition = "2021"

[dependencies]
anyhow = "1.0.75"
configmodel = { version = "0.1.0", path = "../config/model" }
fs-err = { version = "2.6.0", features = ["tokio"] }
identity = { version = "0.1.0", path = "../identity" }
spawn-ext = { version = "0.1.0", path = "../spawn-ext" }
treestate = { version = "0.1.0", path = "../treestate" }
types = { version = "0.1.0", path = "../types" }
