# @generated by autocargo

[package]
name = "spawn-ext"
version = "0.1.0"
edition = "2021"

[dependencies]
libc = "0.2.98"

[dev-dependencies]
tempfile = "3.2"

[target.'cfg(windows)'.dependencies]
winapi = { version = "0.3", features = ["handleapi", "winbase"] }
