# @generated by autocargo

[package]
name = "nonblocking"
version = "0.1.0"
edition = "2021"
license = "MIT"

[dev-dependencies]
futures = { version = "0.3.22", features = ["async-await", "compat"] }
