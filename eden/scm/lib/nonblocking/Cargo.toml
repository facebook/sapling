# @generated by autocargo from //eden/scm/lib/nonblocking:nonblocking
[package]
name = "nonblocking"
edition = "2018"
version = "0.1.0"

[dev-dependencies]
futures = { version = "0.3.5", features = ["async-await", "compat"] }
