# @generated by autocargo from //eden/scm/lib/minibytes:minibytes
[package]
name = "minibytes"
version = "0.1.0"
edition = "2018"

[dependencies]
bytes = { version = "0.5", features = ["serde"], optional = true }
memmap = "0.7"
serde = { version = "=1.0.118", features = ["derive", "rc"] }

[dev-dependencies]
quickcheck = "0.9"

[features]
default = ["frombytes"]
frombytes = ["bytes"]
