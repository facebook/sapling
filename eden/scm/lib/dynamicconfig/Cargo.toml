[package]
name = "dynamicconfig"
version = "0.1.0"
edition = "2018"

[dependencies]
anyhow = "1.0.19"
configparser = { path = "../configparser" }
hgtime = { path = "../hgtime" }
hostname = "0.3"
minibytes = { path = "../minibytes" }
os_info = "2.0.1"

[dev-dependencies]
tempdir = "0.3.7"

[features]
default = []
fb = []
