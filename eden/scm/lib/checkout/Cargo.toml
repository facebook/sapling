# @generated by autocargo from //eden/scm/lib/checkout:checkout
[package]
name = "checkout"
edition = "2018"
version = "0.1.0"
include = ["src/**/*.rs"]

[dependencies]
manifest = { path = "../manifest", features = ["for-tests"] }
types = { path = "../types" }
