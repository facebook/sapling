# @generated by autocargo from //eden/scm/lib/sampling-profiler:sampling-profiler

[package]
name = "sapling-sampling-profiler"
version = "0.1.0"
authors = ["Meta Source Control Team <sourcecontrol-dev@meta.com>"]
edition = "2024"
description = "Sampling profiler based on libunwind."
homepage = "https://sapling-scm.com/"
repository = "https://github.com/facebook/sapling"
license = "MIT"
autoexamples = false

[lib]
name = "sampling_profiler"

[dependencies]
anyhow = "1.0.98"
indexmap = { version = "2.9.0", features = ["arbitrary", "rayon", "serde"] }
libc = "0.2.139"
sapling-ascii-tree = { version = "0.1.0", path = "../util/ascii-tree" }
sapling-backtrace-ext = { version = "0.1.0", path = "../backtrace-ext" }

[dev-dependencies]
quickcheck = "1.0"

[features]
default = []
nightly = []
