# @generated by autocargo from //eden/scm/lib/manifest-augmented-tree:manifest-augmented-tree

[package]
name = "manifest-augmented-tree"
version = "0.1.0"
edition = "2024"

[dependencies]
anyhow = "1.0.98"
base64 = { version = "0.22.1", features = ["alloc"] }
blake3 = { version = "=1.8.2", features = ["mmap", "rayon", "traits-preview"] }
sapling-minibytes = { version = "0.1.0", path = "../minibytes" }
sapling-types = { version = "0.1.0", path = "../types" }

[dev-dependencies]
assert_matches = "1.5"
