# @generated by autocargo from //eden/scm:hg

[package]
name = "hgmain"
version = "0.1.0"
edition = "2024"
build = "build.rs"

[dependencies]
commands = { version = "0.1.0", path = "../../lib/commands", default-features = false }
sapling-atexit = { version = "0.1.0", path = "../../lib/util/atexit" }
sapling-clidispatch = { version = "0.1.0", path = "../../lib/clidispatch" }
sapling-configloader = { version = "0.1.0", path = "../../lib/config/loader" }
tracing = { version = "0.1.41", features = ["attributes", "valuable"] }

[build-dependencies]
cc = "1.2.65"
sapling-python-sysconfig = { version = "0.1.0", path = "../../lib/util/python-sysconfig" }

[target.'cfg(target_os = "linux")'.dependencies]
libc = "0.2.186"

[target.'cfg(target_os = "macos")'.dependencies]
libc = "0.2.186"
sapling-webview-app = { version = "0.1.0", path = "../../lib/webview-app" }

[target.'cfg(target_os = "windows")'.dependencies]
anyhow = "1.0.102"
winapi = { version = "0.3", features = ["everything", "std"] }

[features]
default = []
eden = ["commands/eden", "sapling-clidispatch/eden"]
fb = ["commands/fb"]
sl_oss = ["commands/sl_oss", "sapling-clidispatch/sl_oss"]

[profile.release]
debug = true
lto = false
incremental = true
