[package]
name = "conch_parser"
version = "0.1.0"
edition = "2021"

[lib]
name = "conch_parser"
crate-type = ["cdylib"]

[dependencies]
cpython = { version = "0.7", default-features = false, features = ["extension-module", "serde-convert"] }
conch-parser = { path = "../../lib/third-party/conch-parser" }
