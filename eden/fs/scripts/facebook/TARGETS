load("@fbcode_macros//build_defs:python_binary.bzl", "python_binary")

oncall("scm_client_infra")

python_binary(
    name = "find_changed_dependencies",
    srcs = ["find_changed_dependencies.py"],
    main_module = "eden.fs.scripts.facebook.find_changed_dependencies",
)

python_binary(
    name = "bisect",
    srcs = ["bisect.py"],
    main_module = "eden.fs.scripts.facebook.bisect",
)
