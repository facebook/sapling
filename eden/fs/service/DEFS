def create_eden_fs_rules(suffix, server_srcs, server_deps):
    cpp_binary(
        name = "edenfs" + suffix,
        srcs = [
            "main.cpp",
        ] + server_srcs,
        deps = [
            ":server",
        ] + server_deps,
    )
