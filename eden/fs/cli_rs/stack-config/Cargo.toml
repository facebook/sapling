[package]
name = "stack-config"
version = "0.1.0"
edition = "2018"

[dependencies]
stack-config-derive = { path = "../stack-config-derive" }
serde_derive = "1.0"

[dev-dependencies]
toml = "0.5.8"
serde = { version = "1.0.123", features = ["derive"] }

[[example]]
name = "parse"
