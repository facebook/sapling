# @generated by autocargo from //eden/fs/cli_rs/edenfs-utils:edenfs-utils

[package]
name = "edenfs-utils"
version = "0.1.0"
authors = ["Facebook Source Control Team <sourcecontrol-dev@fb.com>"]
edition = "2021"
license = "GPLv2+"

[dependencies]
anyhow = "1.0.95"
byteorder = "1.3"
edenfs-error = { version = "0.1.0", path = "../edenfs-error" }
sysinfo = "0.30.11"
tracing = { version = "0.1.41", features = ["attributes", "valuable"] }

[dev-dependencies]
lazy_static = "1.4"

[target.'cfg(target_os = "linux")'.dependencies]
nix = { version = "0.29.0", features = ["dir", "event", "hostname", "inotify", "ioctl", "mman", "mount", "net", "poll", "ptrace", "reboot", "resource", "sched", "term", "time", "user", "zerocopy"] }

[target.'cfg(target_os = "macos")'.dependencies]
nix = { version = "0.29.0", features = ["dir", "event", "hostname", "inotify", "ioctl", "mman", "mount", "net", "poll", "ptrace", "reboot", "resource", "sched", "term", "time", "user", "zerocopy"] }

[target.'cfg(target_os = "windows")'.dependencies]
winapi = { version = "0.3", features = ["everything", "std"] }

[target.'cfg(target_os = "windows")'.dev-dependencies]
quickcheck = "1.0"
quickcheck_macros = "1.0"
