query UserHomePageQuery($after: String) {
  viewer {
    pullRequests(first: 100, after: $after, orderBy: {field: UPDATED_AT, direction: DESC}) {
      pageInfo {
        hasNextPage
        endCursor
      }
      nodes {
        ...HomePagePullRequestFragment
      }
    }
  }
}
