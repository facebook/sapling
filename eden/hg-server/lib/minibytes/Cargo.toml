# @generated by autocargo from //eden/hg-server/lib/minibytes:minibytes
[package]
name = "minibytes"
version = "0.1.0"
edition = "2021"

[dependencies]
bytes = { version = "1.1", features = ["serde"], optional = true }
memmap = "0.7"
serde = { version = "1.0.126", features = ["derive", "rc"] }

[dev-dependencies]
quickcheck = "1.0"

[features]
default = ["frombytes"]
frombytes = ["bytes"]
