# @generated by autocargo from //eden/mononoke/common/yield_stream:yield_stream

[package]
name = "yield_stream"
version = "0.1.0"
authors = ["Facebook"]
edition = "2024"
license = "GPLv2+"

[dependencies]
futures = { version = "0.3.31", features = ["async-await", "compat"] }
pin-project = "1.1.10"

[dev-dependencies]
bytes = { version = "1.10", features = ["serde"] }
tokio = { version = "1.47.1", features = ["full", "test-util", "tracing"] }
