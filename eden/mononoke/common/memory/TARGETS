load("@fbcode_macros//build_defs:rust_library.bzl", "rust_library")

oncall("mononoke")

rust_library(
    name = "memory",
    srcs = glob(["src/**/*.rs"]),
    deps = [
        "fbsource//third-party/rust:memory-stats",
    ],
)
