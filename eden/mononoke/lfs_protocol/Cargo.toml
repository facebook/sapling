[package]
name = "lfs_protocol"
edition = "2018"
version = "0.1.0"
authors = ['Facebook']
license = "GPLv2+"
include = ["src/**/*.rs"]

[dependencies]
anyhow = "1.0"
faster-hex = "0.4"
hyper = "0.12" # todo: rename to "hyper-old"
quickcheck = "0.9"
serde = { version = "1.0", features = ["derive", "rc"] }

[dev-dependencies]
assert_matches = "1.3"
maplit = "1.0"
serde_json = "1.0"
