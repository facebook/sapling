[package]
name = "mononoke_types-mocks"
version = "0.1.0"
authors = ["Facebook"]
edition = "2018"
license = "GPLv2+"

[lib]
path = "lib.rs"

[dependencies]
chrono = { version = "0.4", features = ["serde"] }
lazy_static = "1.0"
mononoke_types = { version = "0.1.0", path = ".." }
